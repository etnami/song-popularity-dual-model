# song-popularity-dual-model
Comparing logistic regression and Random Forest on 9 acoustic features to predict song popularity: both near chance (AUC ~0.51), but Random Forest actually catches popular songs (9% sensitivity) where logistic regression catches zero.
